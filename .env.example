# Local env (example). For production, set these in Vercel Environment Variables.

# Prisma (Supabase Postgres)
# - DATABASE_URL: use Supabase pooled/transaction (pgbouncer) URL (usually port 6543)
# - DIRECT_URL:  use Supabase direct/session URL (usually port 5432) for Prisma migrate
DATABASE_URL="PASTE_POSTGRES_PRISMA_URL_HERE"
DIRECT_URL="PASTE_POSTGRES_URL_NON_POOLING_HERE"

# JWT (for custom username/password session)
# Generate a long random secret (>= 32 chars).
JWT_SECRET="PASTE_LONG_RANDOM_SECRET_HERE"

# Encrypt marketplace credentials/tokens stored in DB (optional; falls back to JWT_SECRET)
MARKETPLACE_SECRET=""

# Optional
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Base URL used for redirect after OAuth callback (optional)
APP_BASE_URL="http://localhost:3000"

# Shopee Open Platform (Production)
# Fill in after your app is approved and you receive production Partner ID/Key.
SHOPEE_PARTNER_ID=""
SHOPEE_PARTNER_KEY=""
SHOPEE_HOST="partner.shopeemobile.com"

# Full callback URL (must be under your Live Redirect URL Domain)
SHOPEE_REDIRECT_URL="http://localhost:3000/api/integrations/shopee/callback"

# Optional: if you build your own webhook receiver security
WEBHOOK_SECRET_SHOPEE=""

# Stock alert
LOW_STOCK_THRESHOLD=5

# Realtime Notif with Pusher.com (optional)
PUSHER_APP_ID=""
PUSHER_KEY=""
PUSHER_SECRET=""
PUSHER_CLUSTER="ap1"

NEXT_PUBLIC_PUSHER_KEY=""
NEXT_PUBLIC_PUSHER_CLUSTER="ap1"
